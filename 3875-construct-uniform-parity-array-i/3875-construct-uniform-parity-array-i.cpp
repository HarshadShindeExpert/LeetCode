class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        return true ;
        // if any nmber in nums1 is odd then nums2 will contain all odd numbers .
        // because if number is odd then keep it and if number is even then substract from odd number .

        
        // if all numbers in nums1 are even then nums2 will contain all even numbers .
        
    }
};